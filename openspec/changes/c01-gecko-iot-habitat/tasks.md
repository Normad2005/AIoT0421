## 1. 基礎架構與計時器

- [ ] 1.1 安裝並引入 Jack Christensen 的 `Timer` 程式庫。
- [ ] 1.2 設定 `Setup()` 函式，初始化 Serial 波特率與各引腳模式（A0, A1, Pin 2, Pin 9）。
- [ ] 1.3 在 `Loop()` 中設置 `t.update()` 並配置 2000ms 的採樣任務回呼。

## 2. 感測與驅動邏輯實現

- [ ] 2.1 實作溫濕度讀取函式，包含 DHT11 的非阻塞採樣。
- [ ] 2.2 實作光源追蹤算法：計算 A0/A1 差值並實現 Threshold 判定 logic。
- [ ] 2.3 配置伺服馬達 PWM 輸出，根據差值方向進行步進微調。

## 3. 通訊與警報系統

- [ ] 3.1 格式化 CSV 資料字串並透過 HC-06 發送至 Serial。
- [ ] 3.2 實作溫度/濕度安全閾值檢查邏輯。
- [ ] 3.3 測試並驗證系統在多任務併行下的穩定性（不使用 `delay()`）。
